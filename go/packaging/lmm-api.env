# Deliberately non-secret package default.
#
# The existing production file is snapshotted before a package upgrade and
# restored afterwards. Configure secrets only in /etc/lmm-api/lmm-api.env on
# the host; never add them to this package or source repository.
